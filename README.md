# Svelte Frontend

## Run

```bash
cd frontend
npm install
npm run dev
```

## Environment

Create a `.env` file if your FastAPI backend is not running at the default address:

```bash
VITE_API_BASE_URL=http://127.0.0.1:8000
```
